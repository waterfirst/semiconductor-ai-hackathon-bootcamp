# Design

## Direction

공정 엔지니어가 익숙한 process window map과 실험 노트를 결합한다. 페이지는 홍보물이 아니라 수업 중 계속 열어 두는 작업 화면이다.

## Visual System

- 밝은 회색 측정 용지 위에 짙은 남색 잉크를 기본으로 사용한다.
- plasma cyan, RF magenta, warning amber는 데이터 계열과 상태에만 사용한다.
- 장식용 그림 대신 축, 격자, 신호선, 번호, 주석으로 정보를 구조화한다.
- 둥근 카드의 반복을 피하고 선·필드·여백으로 구획한다.

## Typography

- 한글 본문은 시스템 산세리프를 사용해 설치 의존성을 없앤다.
- 수치, 시간, 명령, 변수명은 고정폭 글꼴로 구분한다.
- 제목은 과장하지 않고 좁은 행간과 강한 굵기로 기술 문서의 권위를 만든다.

## Interaction

- 상단 네비게이션은 현재 읽는 구간을 표시한다.
- 120분 Lecture Map은 클릭하면 해당 상세 단계로 이동한다.
- 오늘 실습 경로는 문제·완성본·원본 프롬프트·정제 가이드를 번호가 있는 링크형 필드로 연결하고, 도구별 레인은 같은 완료 기준 아래 병렬로 구분한다.
- 여덟 단계는 문제 파악에서 GitHub Pages 배포까지 같은 번호와 순서로 반복한다.
- 문제발견 도구는 입력 내용을 브라우저 안에서 한 문장 프로젝트 정의로 조합한다.
- 주제 탐색기는 결론 대신 토론 질문과 A/B 학습·Holdout 데이터 다운로드를 제공한다.
- 명령은 한 번에 복사할 수 있다.
- 동작 줄이기 설정에서는 모든 애니메이션을 제거한다.

## Teaching Continuity

- 1회 120분 수업에서는 문제·데이터·계획·Agent 분석의 전체 흐름을 한 번 경험한다.
- 8월 20일 이후에는 새 CSV를 받는 Streamlit 앱, 독립형 HTML 보고서, README·SKILL 문서와 공개 URL까지 같은 흐름을 자율 반복한다.
- 화려한 결과보다 데이터 감사, Holdout 검증, 실패 사례와 사람의 수정 판단을 우선한다.

## Responsive Rules

- 데스크톱은 비대칭 12열, 모바일은 단일 열이다.
- 라이브 경로는 데스크톱 4열, 태블릿 2열, 모바일 1열로 전환하며 도구·현장 운영 레인은 모바일에서 1열로 쌓는다.
- 시간축은 모바일에서 수평 스크롤하지 않고 세로 목록으로 전환한다.
- 최소 터치 영역은 44px, 본문은 최소 16px을 유지한다.

## Dual Industry Galaxy

- 반도체는 cyan 고리·발광, 디스플레이는 magenta 고리·발광, 양 산업 공통 장비·소재는 amber 고리·발광으로 표시한다.
- 행성 본체는 제조·장비·소재·패키징·설계 카테고리 색을 사용하고, 서로 다른 입체 도형을 함께 써 색각 차이가 있어도 구분하게 한다.
- 각 산업 은하 안에서 세트·고객·설계 → 패널·반도체 제조 → 설비·패키징·테스트 → 재료·핵심부품을 서로 다른 Z축 좌표에 배치한다.
- 행성 애니메이션은 Z축 공급망 밴드를 벗어나지 않고 해당 레이어 안에서만 천천히 좌우 이동한다.
- 행성 크기는 최근 공개 연매출의 넓은 5개 구간을 뜻한다. 비교 가능한 수치가 없는 비상장사·사업부는 추정하지 않고 중립 기본 크기로 둔다.
- 기본 화면에서는 연매출 $10B 이상과 산업 핵심기업 이름만 표시한다. 그보다 작은 행성은 선택할 때만 회사명을 표시해 은하 구조와 연결선의 가독성을 지킨다.
- 공통 기업은 양쪽에 복제하지 않고 중앙 브리지에 하나의 노드로 둔다.
- 회사 노드의 형상은 업종 분류, 색상은 산업 도메인을 뜻한다.
- 일반 은하 연결선은 교육용 공정 관계이고, 공식 발표로 확인된 거래·협력만 분홍 실선으로 구분한다.
- 모바일은 선택하지 않은 3D 라벨을 숨기고 상세보고서를 단일 열로 제공한다.
